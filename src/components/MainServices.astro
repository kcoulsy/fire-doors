---
import CheckedBox from "../icons/CheckedBox.astro";
const { location } = Astro.params;

function capitalizeFirstLetter(string: string) {
  return string.charAt(0).toUpperCase() + string.slice(1);
}

---

<!--
  Heads up! 👋

  This component comes with some `rtl` classes. Please remove them if they are not needed in your project.
-->
<section>
  <div
    class="max-w-screen-xl mx-auto px-4 py-8 sm:px-6 sm:py-12 lg:px-8 lg:py-16"
  >
    <div class="flex flex-col lg:items-center lg:gap-x-16">
      <div
        class="mx-auto max-w-lg text-center lg:mx-0 ltr:lg:text-left rtl:lg:text-right"
      >
        <h3 class="text-3xl font-bold sm:text-4xl">What we can offer {location ? `in ${capitalizeFirstLetter(location)}` : null }</h3>

        <p class="mt-4 text-gray-600">
          We offer a range of services to ensure your fire doors are compliant
          and safe { location ? `in ${capitalizeFirstLetter(location)}` : null }.
        </p>

        <!-- <div
         
          class="mt-8 inline-block rounded bg-indigo-600 px-12 py-3 text-sm font-medium text-white transition hover:bg-indigo-700 focus:outline-none focus:ring focus:ring-yellow-400"
        >
          Get Started Today
        </a> -->
      </div>

      <div class="grid grid-cols-2 gap-4 sm:grid-cols-3 col-span-2">
        <div
          class="flex flex-col items-center justify-center rounded-xl border border-gray-100 p-4 shadow-sm hover:border-gray-200 hover:ring-1 hover:ring-gray-200 focus:outline-none focus:ring"
        >
          <CheckedBox className="h-6 w-6" />

          <h3 class="mt-2 font-semibold">Fire door maintenance and repairs</h3>
        </div>
        <div
          class="flex flex-col items-center justify-center rounded-xl border border-gray-100 p-4 shadow-sm hover:border-gray-200 hover:ring-1 hover:ring-gray-200 focus:outline-none focus:ring"
        >
          <CheckedBox className="h-6 w-6" />

          <h3 class="mt-2 font-bold">Surveys, assessments and checks</h3>
        </div>

        <div
          class="flex flex-col items-center justify-center rounded-xl border border-gray-100 p-4 shadow-sm hover:border-gray-200 hover:ring-1 hover:ring-gray-200 focus:outline-none focus:ring"
        >
          <CheckedBox className="h-6 w-6" />

          <h3 class="mt-2 font-bold">
            6 monthly inspections to RRO (Regulatory Reform Order)
          </h3>
        </div>
        <div
          class="flex flex-col items-center justify-center rounded-xl border border-gray-100 p-4 shadow-sm hover:border-gray-200 hover:ring-1 hover:ring-gray-200 focus:outline-none focus:ring"
        >
          <CheckedBox className="h-6 w-6" />

          <h3 class="mt-2 font-bold">Supply and installation of fire doors and sets</h3>
        </div>
        <div
          class="flex flex-col items-center justify-center rounded-xl border border-gray-100 p-4 shadow-sm hover:border-gray-200 hover:ring-1 hover:ring-gray-200 focus:outline-none focus:ring"
        >
          <CheckedBox className="h-6 w-6" />

          <h3 class="mt-2 font-bold">Fire door inspections</h3>
        </div>

        <div
          class="flex flex-col items-center justify-center rounded-xl border border-gray-100 p-4 shadow-sm hover:border-gray-200 hover:ring-1 hover:ring-gray-200 focus:outline-none focus:ring"
        >
          <CheckedBox className="h-6 w-6" />

          <h3 class="mt-2 font-bold">Fire door risk assesments</h3>
        </div>
      </div>
    </div>
  </div>
</section>
